{
  "name": "@workspace/locktest",
  "version": "1.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "CI=1 npx expo start --port $PORT",
    "dev": "CI=1 npx expo start --port $PORT",
    "android": "CI=1 npx expo start --android --port $PORT",
    "ios": "CI=1 npx expo start --ios --port $PORT",
    "build": "npx expo export",
    "serve": "CI=1 npx expo start --no-dev --port $PORT",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "expo": "~54.0.34",
    "expo-font": "~14.0.10",
    "expo-keep-awake": "^15.0.8",
    "expo-linking": "~8.0.10",
    "expo-router": "~6.0.17",
    "expo-splash-screen": "~31.0.12",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-gesture-handler": "~2.28.0",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@expo/cli": "54.0.23",
    "@expo/config-plugins": "^10.0.0",
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  }
}
